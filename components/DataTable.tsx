interface DataTableProps {
  data?: any
  columns?: any
  searchable?: any
  sortable?: any
  pagination?: any
}

export default function DataTable({ data, columns, searchable, sortable, pagination }: DataTableProps) {
  return (
    <div className="">
      {/* DataTable component content */}
    </div>
  )
}