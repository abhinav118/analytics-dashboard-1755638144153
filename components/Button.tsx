interface ButtonProps {
  variant?: any
  size?: any
  disabled?: any
  loading?: any
}

export default function Button({ variant, size, disabled, loading }: ButtonProps) {
  return (
    <div className="">
      {/* Button component content */}
    </div>
  )
}